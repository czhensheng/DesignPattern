官方定义：
确保一个类只有一个实例，并且提供一个全局访问点。
简单描述：
保证一个类只有一个实例的实现方法，且该实例易于外界访问

生活中的实例：
1、windows中就只能有一个任务管理器


优点：
1、单例模式会阻止其他对象实例化其自己的单例对象的副本，从而确保所有对象都访问唯一实例
2、因为类控制了实例化过程，所以类可以灵活更改实例过程
缺点：
1、虽然数量少，但是每次对对象的引用的时候都需要检查类的实例这操作，仍然需要一定的开销（可以使用静态变量初始化来解决该问题）
2、实例类会被取消内存管理的分配（静态类在整个应用程序域生命周期内, 一直保留在内存中.）